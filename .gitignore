.DS_Store
.env
.env.*
*.local

# Secrets and credentials
credentials.json
secrets.*

# Build artifacts
.pio/
build/
dist/
sdkconfig.old
managed_components/

# Local-only resources (vendor firmware, zips, PDFs, large binaries — not in git)
resources/**
!resources/README.md

# Vendor trees belong in resources/; ignore extracted copies under mcs/
ZENSORIA/mcs/*/ESP32-C6-GEEK/
ZENSORIA/mcs/*/Firmware/
