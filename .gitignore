.DS_Store
.env
.env.*
*.local

# Secrets and credentials
credentials.json
secrets.*


# Local-only resources (vendor firmware, zips, PDFs, large binaries — not in git)
resources/**
!resources/README.md

scritpts/